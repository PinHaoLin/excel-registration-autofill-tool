# Rename Project Folder

## Summary
Rename the project folder to `excel-registration-autofill-tool` so it can be used as a clear GitHub repository name.

## Motivation
The current folder name is Chinese and contains spaces in the parent path. For GitHub publishing and repository clarity, the project should use a lowercase English name with hyphen separators.

## Scope
- Rename the project folder from `自動填寫工具` to `excel-registration-autofill-tool`.
- Preserve all existing project files and source history.

## Out of Scope
- No application behavior changes.
- No UI text changes.
- No package metadata changes unless requested separately.
