# Tasks

- [x] Record the GitHub-ready target project name.
- [x] Attempt to rename the active project folder.
- [x] Detect that Windows is blocking the live rename because the folder is in use.
- [x] Add a repeatable helper script for completing the rename after handles are released.
- [x] Update the current-state OpenSpec project naming specification.
- [ ] Complete the folder rename after the active workspace handle is released.
